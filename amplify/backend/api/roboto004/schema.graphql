type Bot @model @auth(rules: [{allow: public}]) {
  id: ID!
  priceLow: Float
  priceHigh: Float
  numSlices: Int
  active: Boolean
  priceEntry: Float
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}
 